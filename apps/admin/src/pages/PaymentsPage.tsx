import { Check, Eye, FileImage, X } from "lucide-react";
import { useState } from "react";
import { AdminModal } from "../components/AdminModal";
import { PageHeader } from "../components/PageHeader";
import { PaymentBadge } from "../components/PaymentBadge";
import { formatPkr, payments as initialPayments } from "../data/adminData";
import type { Payment } from "../types/admin";

export function PaymentsPage() {
  const [payments, setPayments] = useState(initialPayments);
  const [selected, setSelected] = useState<Payment | null>(null);
  const update = (id: string, status: Payment["status"]) => setPayments((current) => current.map((payment) => payment.id === id ? { ...payment, status } : payment));
  return <><PageHeader eyebrow="FINANCE OPERATIONS" title="Payment proofs" description="Review submitted payment references before updating booking payment status."/><section className="overflow-hidden rounded-lg border border-border bg-white"><div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[850px] text-left"><thead className="border-b border-border bg-mist text-xs uppercase text-stone"><tr><th className="px-4 py-3">Booking</th><th className="px-4 py-3">Tourist</th><th className="px-4 py-3">Amount</th><th className="px-4 py-3">Method</th><th className="px-4 py-3">Status</th><th className="px-4 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-border">{payments.map((payment) => <tr key={payment.id} className="hover:bg-snow"><td className="px-4 py-3 text-sm font-bold text-river">{payment.bookingReference}</td><td className="px-4 py-3 text-sm font-semibold text-charcoal">{payment.touristName}</td><td className="px-4 py-3 text-sm text-charcoal">{formatPkr(payment.amount)}</td><td className="px-4 py-3 text-sm text-stone">{payment.method}</td><td className="px-4 py-3"><PaymentBadge status={payment.status}/></td><td className="px-4 py-3"><div className="flex justify-end gap-2"><button type="button" className="icon-button" onClick={() => setSelected(payment)} aria-label="View payment proof"><Eye size={16}/></button><button type="button" className="icon-button text-pine" onClick={() => update(payment.id, "Verified")} aria-label="Verify payment"><Check size={16}/></button><button type="button" className="icon-button text-[#9c3f2e]" onClick={() => update(payment.id, "Rejected")} aria-label="Reject payment"><X size={16}/></button></div></td></tr>)}</tbody></table></div><div className="divide-y divide-border md:hidden">{payments.map((payment) => <article key={payment.id} className="p-4"><div className="flex justify-between gap-3"><div><span className="text-xs font-bold text-river">{payment.bookingReference}</span><h2 className="mt-1 font-semibold text-charcoal">{payment.touristName}</h2></div><PaymentBadge status={payment.status}/></div><dl className="mt-3 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-xs text-stone">Amount</dt><dd className="mt-1 font-semibold">{formatPkr(payment.amount)}</dd></div><div><dt className="text-xs text-stone">Method</dt><dd className="mt-1 font-semibold">{payment.method}</dd></div></dl><div className="mt-4 flex gap-2"><button type="button" className="button-secondary" onClick={() => setSelected(payment)}><Eye size={15}/> Proof</button><button type="button" className="button-primary ml-auto" onClick={() => update(payment.id, "Verified")}><Check size={15}/> Verify</button><button type="button" className="button-secondary text-[#9c3f2e]" onClick={() => update(payment.id, "Rejected")}><X size={15}/> Reject</button></div></article>)}</div></section><AdminModal open={Boolean(selected)} onClose={() => setSelected(null)} title={`Payment proof · ${selected?.bookingReference ?? ""}`} description="Static proof placeholder">{selected && <><div className="grid min-h-60 place-items-center rounded-lg border border-dashed border-border bg-snow text-center"><div><FileImage className="mx-auto text-river" size={30}/><strong className="mt-3 block text-sm text-charcoal">Payment proof placeholder</strong><p className="mt-1 text-xs text-stone">{selected.referenceNumber} · {selected.method}</p></div></div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div className="rounded-md bg-snow p-3"><dt className="text-xs text-stone">Amount</dt><dd className="mt-1 font-semibold">{formatPkr(selected.amount)}</dd></div><div className="rounded-md bg-snow p-3"><dt className="text-xs text-stone">Submitted</dt><dd className="mt-1 font-semibold">{selected.submittedAt}</dd></div></dl></>}</AdminModal></>;
}
