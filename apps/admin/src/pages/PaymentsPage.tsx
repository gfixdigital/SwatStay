import { Check, Eye, FileImage, X } from "lucide-react";
import { useMemo, useState } from "react";
import { AdminModal } from "../components/AdminModal";
import { PageHeader } from "../components/PageHeader";
import { PaymentBadge } from "../components/PaymentBadge";
import { formatPkr, payments as initialPayments } from "../data/adminData";
import type { Payment } from "../types/admin";

export function PaymentsPage() {
  const [payments, setPayments] = useState(initialPayments);
  const [selected, setSelected] = useState<Payment | null>(null);
  const [status, setStatus] = useState("All"); const [method, setMethod] = useState("All"); const [date, setDate] = useState(""); const [note, setNote] = useState("");
  const visible = useMemo(() => payments.filter((item) => (status === "All" || item.status === status) && (method === "All" || item.method === method) && (!date || item.submittedAt.startsWith(date))), [payments, status, method, date]);
  const update = (id: string, next: Payment["status"]) => setPayments((current) => current.map((payment) => payment.id === id ? { ...payment, status: next, verifiedBy: next === "Verified" ? "Current admin" : payment.verifiedBy, verifiedAt: next === "Verified" ? "Just now" : payment.verifiedAt } : payment));
  return <><PageHeader eyebrow="FINANCE OPERATIONS" title="Payment proofs" description="Review submitted payment references before updating booking payment status."/><div className="mb-4 grid gap-3 rounded-lg border border-border bg-white p-3 md:grid-cols-3"><select className="field" value={status} onChange={(e) => setStatus(e.target.value)}><option>All</option><option>Proof submitted</option><option>Verified</option><option>Rejected</option></select><select className="field" value={method} onChange={(e) => setMethod(e.target.value)}><option>All</option>{[...new Set(payments.map((item) => item.method))].map((value) => <option key={value}>{value}</option>)}</select><input type="date" className="field" value={date} onChange={(e) => setDate(e.target.value)}/></div><section className="overflow-hidden rounded-lg border border-border bg-white"><div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[980px] text-left"><thead className="border-b border-border bg-mist text-xs uppercase text-stone"><tr><th className="px-4 py-3">Booking</th><th className="px-4 py-3">Tourist</th><th className="px-4 py-3">Amount</th><th className="px-4 py-3">Method</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Verified by</th><th className="px-4 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-border">{visible.map((payment) => <tr key={payment.id} className="hover:bg-snow"><td className="px-4 py-3 text-sm font-bold text-river">{payment.bookingReference}</td><td className="px-4 py-3 text-sm font-semibold text-charcoal">{payment.touristName}</td><td className="px-4 py-3 text-sm text-charcoal">{formatPkr(payment.amount)}</td><td className="px-4 py-3 text-sm text-stone">{payment.method}</td><td className="px-4 py-3"><PaymentBadge status={payment.status}/></td><td className="px-4 py-3 text-xs text-stone">{payment.verifiedBy ?? "Not verified"}<span className="block">{payment.verifiedAt}</span></td><td className="px-4 py-3"><div className="flex justify-end gap-2"><button type="button" className="icon-button" onClick={() => { setSelected(payment); setNote(payment.note ?? ""); }} aria-label="View payment proof"><Eye size={16}/></button><button type="button" className="icon-button text-pine" onClick={() => update(payment.id, "Verified")} aria-label="Verify payment"><Check size={16}/></button><button type="button" className="icon-button text-[#9c3f2e]" onClick={() => update(payment.id, "Rejected")} aria-label="Reject payment"><X size={16}/></button></div></td></tr>)}</tbody></table></div><div className="divide-y divide-border md:hidden">{visible.map((payment) => <article key={payment.id} className="p-4"><div className="flex justify-between gap-3"><div><span className="text-xs font-bold text-river">{payment.bookingReference}</span><h2 className="mt-1 font-semibold text-charcoal">{payment.touristName}</h2></div><PaymentBadge status={payment.status}/></div><p className="mt-3 text-sm text-stone">{formatPkr(payment.amount)} · {payment.method}</p><div className="mt-4 flex gap-2"><button type="button" className="button-secondary" onClick={() => { setSelected(payment); setNote(payment.note ?? ""); }}><Eye size={15}/> Proof</button><button type="button" className="button-primary ml-auto" onClick={() => update(payment.id, "Verified")}><Check size={15}/> Verify</button><button type="button" className="button-secondary text-[#9c3f2e]" onClick={() => update(payment.id, "Rejected")}><X size={15}/> Reject</button></div></article>)}</div></section><AdminModal open={Boolean(selected)} onClose={() => setSelected(null)} title={`Payment proof · ${selected?.bookingReference ?? ""}`} description="Traveler-submitted frontend sample">{selected && <><div className="grid min-h-52 place-items-center rounded-lg border border-dashed border-border bg-snow text-center"><div><FileImage className="mx-auto text-river" size={30}/><strong className="mt-3 block text-sm text-charcoal">{selected.proofFileName ?? "Payment proof file"}</strong><p className="mt-1 text-xs text-stone">{selected.referenceNumber} · {selected.method}</p><p className="mt-1 text-xs text-stone">{formatPkr(selected.amount)} · submitted {selected.submittedAt}</p><span className="mt-3 inline-flex rounded-md border border-border bg-white px-2 py-1 text-[11px] font-semibold text-stone">File preview requires storage backend</span></div></div><label className="label mt-4">Finance note<textarea className="field mt-1.5 min-h-24 py-3" value={note} onChange={(e) => setNote(e.target.value)}/></label><button type="button" className="button-primary mt-3 w-full" onClick={() => { setPayments((current) => current.map((item) => item.id === selected.id ? { ...item, note } : item)); setSelected(null); }}>Save note</button></>}</AdminModal></>;
}
