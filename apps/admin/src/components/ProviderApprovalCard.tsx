import { Building2, Check, Eye, FileText, Images, MapPin, X } from "lucide-react";
import type { Provider } from "../types/admin";
import { StatusBadge } from "./StatusBadge";

export function ProviderApprovalCard({ provider, onView, onApprove, onReject }: { provider: Provider; onView: () => void; onApprove: () => void; onReject: () => void }) {
  return <article className="rounded-lg border border-border bg-white p-4"><div className="flex items-start justify-between gap-3"><div className="flex min-w-0 gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-md bg-mist text-river"><Building2 size={19}/></span><div className="min-w-0"><h2 className="truncate text-base font-bold text-charcoal">{provider.name}</h2><p className="mt-1 flex items-center gap-1 text-sm text-stone"><MapPin size={13}/>{provider.location} · {provider.serviceType}</p></div></div><StatusBadge status={provider.status}/></div><div className="mt-4 grid grid-cols-3 gap-2 rounded-md bg-snow p-3 text-xs"><span><small className="block text-stone">Documents</small><strong className="mt-1 flex items-center gap-1 text-charcoal"><FileText size={12}/>{provider.documentsStatus}</strong></span><span><small className="block text-stone">Photos</small><strong className="mt-1 flex items-center gap-1 text-charcoal"><Images size={12}/>{provider.photosStatus}</strong></span><span><small className="block text-stone">Commission</small><strong className="mt-1 block text-charcoal">{provider.commissionRate}%</strong></span></div><div className="mt-4 flex flex-wrap gap-2"><button type="button" className="button-secondary" onClick={onView}><Eye size={15}/> View details</button><button type="button" className="button-secondary ml-auto text-[#9c3f2e]" onClick={onReject} disabled={provider.status !== "Pending review"}><X size={15}/> Reject</button><button type="button" className="button-primary" onClick={onApprove} disabled={provider.status !== "Pending review"}><Check size={15}/> Approve</button></div></article>;
}
