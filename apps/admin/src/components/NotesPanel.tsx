import { useState } from "react";

export function NotesPanel({ initialValue = "", title = "Internal notes" }: { initialValue?: string; title?: string }) { const [value, setValue] = useState(initialValue); const [saved, setSaved] = useState(false); return <section className="panel"><div className="panel-header"><div><h2 className="panel-title">{title}</h2><p className="panel-subtitle">Frontend state only</p></div></div><div className="p-4"><textarea className="field min-h-28 resize-y py-3" value={value} onChange={(event) => { setValue(event.target.value); setSaved(false); }} placeholder="Add an operations note"/><div className="mt-3 flex items-center justify-between"><span className="text-xs text-stone">{saved ? "Saved in this session" : "Not persisted"}</span><button type="button" className="button-secondary" onClick={() => setSaved(true)}>Save note</button></div></div></section>; }
