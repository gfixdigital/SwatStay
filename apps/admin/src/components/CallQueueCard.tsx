import { CheckCircle2, MessageCircle, NotebookPen, PhoneCall } from "lucide-react";
import type { Booking } from "../types/admin";

export function CallQueueCard({ booking, confirmed, onNotes, onConfirm }: { booking: Booking; confirmed: boolean; onNotes: () => void; onConfirm: () => void }) {
  const priorityTone = booking.priority === "Urgent" ? "bg-[#fff0ed] text-[#9c3f2e]" : booking.priority === "High" ? "bg-[#fbf3e6] text-[#925d19]" : "bg-mist text-pine";
  return <article className="rounded-lg border border-border bg-white p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><span className="text-xs font-bold text-river">{booking.reference}</span><span className={`rounded-md px-2 py-1 text-xs font-semibold ${priorityTone}`}>{booking.priority}</span></div><h2 className="mt-2 text-base font-bold text-charcoal">{booking.touristName}</h2><p className="mt-1 text-sm text-stone">{booking.packageName}</p></div>{confirmed && <span className="inline-flex items-center gap-1 rounded-md bg-[#e8f1ec] px-2 py-1 text-xs font-semibold text-pine"><CheckCircle2 size={13}/> Confirmed</span>}</div><dl className="mt-4 grid grid-cols-2 gap-3 border-y border-border py-3 text-sm"><div><dt className="text-xs text-stone">Preferred language</dt><dd className="mt-1 font-semibold text-charcoal">{booking.preferredLanguage}</dd></div><div><dt className="text-xs text-stone">Last contact attempt</dt><dd className="mt-1 font-semibold text-charcoal">{booking.lastContactAttempt}</dd></div></dl><div className="mt-4 flex flex-wrap gap-2"><a href={`tel:${booking.phone.replace(/\s/g, "")}`} className="button-primary"><PhoneCall size={15}/> Call</a><a href={`https://wa.me/${booking.phone.replace(/\D/g, "")}`} target="_blank" rel="noreferrer" className="button-secondary"><MessageCircle size={15}/> WhatsApp</a><button type="button" className="button-secondary" onClick={onNotes}><NotebookPen size={15}/> Call notes</button><button type="button" className="button-secondary ml-auto" onClick={onConfirm} disabled={confirmed}><CheckCircle2 size={15}/> {confirmed ? "Confirmed" : "Mark confirmed"}</button></div></article>;
}
