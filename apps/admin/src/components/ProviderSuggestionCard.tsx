import { Check, MapPin } from "lucide-react";
import type { ProviderSuggestion } from "../types/admin";
import { formatPkr } from "../data/adminData";

export function ProviderSuggestionCard({ provider, selected, onSelect }: { provider: ProviderSuggestion; selected: boolean; onSelect: () => void }) {
  return <article className={`rounded-lg border bg-white p-4 ${selected ? "border-river ring-1 ring-river" : "border-border"}`}><div className="flex items-start justify-between gap-3"><div><span className="text-xs font-semibold text-river">{provider.serviceType}</span><h3 className="mt-1 text-base font-bold text-charcoal">{provider.name}</h3><p className="mt-1 flex items-center gap-1 text-xs text-stone"><MapPin size={12}/>{provider.location}</p></div><span className="rounded-md bg-mist px-2 py-1 text-xs font-bold text-pine">{provider.matchScore}% match</span></div><dl className="mt-4 grid grid-cols-2 gap-x-4 gap-y-3 border-y border-border py-3 text-xs"><div><dt className="text-stone">Capacity</dt><dd className="mt-1 font-semibold text-charcoal">{provider.capacity}</dd></div><div><dt className="text-stone">Price</dt><dd className="mt-1 font-semibold text-charcoal">{formatPkr(provider.price)}</dd></div><div><dt className="text-stone">Commission</dt><dd className="mt-1 font-semibold text-charcoal">{provider.commissionRate}%</dd></div><div><dt className="text-stone">Availability</dt><dd className="mt-1 font-semibold text-charcoal">{provider.availability}</dd></div></dl><button type="button" className={selected ? "button-secondary mt-4 w-full border-river text-river" : "button-primary mt-4 w-full"} onClick={onSelect}>{selected && <Check size={15}/>} {selected ? "Selected" : `Select ${provider.serviceType.toLowerCase()}`}</button></article>;
}
