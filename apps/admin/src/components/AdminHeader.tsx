import { useMemo, useState } from "react";
import { AnimatePresence, motion } from "framer-motion";
import { Bell, ChevronDown, Languages, Menu, PackagePlus, Plus, Search, UserRound } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { bookings, payments, providers, supportTickets } from "../data/adminData";

type Result = { label: string; detail: string; to: string };
const notifications = [{ title: "Payment proof awaiting review", detail: "SS-2048 · Ayesha Khan", to: "/payments" }, { title: "Urgent confirmation call", detail: "SS-2053 · Chinese support requested", to: "/call-queue" }, { title: "Provider document needs review", detail: "Bahrain Family Restaurant", to: "/providers/approvals" }];
export function AdminHeader({ onMenuClick }: { onMenuClick: () => void }) {
  const navigate = useNavigate(); const [createOpen, setCreateOpen] = useState(false); const [profileOpen, setProfileOpen] = useState(false); const [language, setLanguage] = useState("EN"); const [query, setQuery] = useState(""); const [notificationsOpen, setNotificationsOpen] = useState(false); const [read, setRead] = useState<string[]>([]);
  const results = useMemo<Result[]>(() => { const q = query.trim().toLowerCase(); if (!q) return []; return [...bookings.map((item) => ({ label: `${item.reference} · ${item.touristName}`, detail: `${item.packageName} · ${item.destination}`, to: `/bookings/${item.id}` })), ...providers.map((item) => ({ label: item.name, detail: `${item.serviceType} · ${item.location}`, to: "/providers" })), ...payments.map((item) => ({ label: `Payment · ${item.bookingReference}`, detail: `${item.touristName} · ${item.status}`, to: "/payments" })), ...supportTickets.map((item) => ({ label: `${item.id} · ${item.issueType}`, detail: `${item.touristName} · ${item.status}`, to: "/support" }))].filter((item) => `${item.label} ${item.detail}`.toLowerCase().includes(q)).slice(0, 6); }, [query]);
  const go = (to: string) => { setQuery(""); setNotificationsOpen(false); navigate(to); };
  return <header className="sticky top-0 z-20 flex min-h-16 items-center justify-between gap-3 border-b border-border bg-white px-4 py-2 md:px-6"><div className="flex min-w-0 items-center gap-3"><button type="button" onClick={onMenuClick} className="icon-button lg:hidden" aria-label="Open navigation"><Menu size={19}/></button><div className="relative hidden min-w-[220px] max-w-md flex-1 sm:block lg:min-w-[280px]"><div className="flex items-center gap-2 rounded-md border border-border bg-snow px-3"><Search size={16} className="text-stone"/><input value={query} onChange={(event) => setQuery(event.target.value)} className="h-9 w-full bg-transparent text-sm outline-none" placeholder="Search booking, tourist, provider" aria-label="Search admin workspace"/></div><AnimatePresence>{query && <motion.div initial={{ opacity: 0, y: -4 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -4 }} className="absolute left-0 right-0 top-full z-30 mt-2 overflow-hidden rounded-lg border border-border bg-white shadow-panel">{results.length ? results.map((result) => <button type="button" key={`${result.label}-${result.to}`} onClick={() => go(result.to)} className="block w-full border-b border-border px-3 py-2.5 text-left last:border-0 hover:bg-mist"><strong className="block text-sm text-charcoal">{result.label}</strong><span className="block text-xs text-stone">{result.detail}</span></button>) : <p className="p-3 text-sm text-stone">No booking, provider, payment, or ticket matches.</p>}</motion.div>}</AnimatePresence></div></div><div className="flex items-center gap-2"><div className="hidden items-center gap-1 rounded-md border border-border p-1 md:flex"><Languages size={15} className="ml-1 text-river"/><select value={language} onChange={(e) => setLanguage(e.target.value)} className="h-7 bg-transparent pr-1 text-xs font-semibold text-charcoal outline-none" aria-label="Admin language"><option>EN</option><option>UR</option><option>ZH</option></select></div><div className="relative"><button type="button" className="button-primary" onClick={() => setCreateOpen((value) => !value)}><Plus size={15}/><span className="hidden sm:inline">Quick create</span><ChevronDown size={14}/></button><AnimatePresence>{createOpen && <motion.div initial={{ opacity: 0, y: -5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -4 }} transition={{ duration: .15 }} className="absolute right-0 top-full mt-2 w-52 rounded-lg border border-border bg-white p-1 shadow-panel"><Link to="/packages" onClick={() => setCreateOpen(false)} className="flex items-center gap-2 rounded-md px-3 py-2 text-sm font-semibold hover:bg-mist"><PackagePlus size={15} className="text-river"/>Create package</Link><Link to="/destinations" onClick={() => setCreateOpen(false)} className="flex items-center gap-2 rounded-md px-3 py-2 text-sm font-semibold hover:bg-mist"><Plus size={15} className="text-river"/>Add destination</Link><Link to="/team" onClick={() => setCreateOpen(false)} className="flex items-center gap-2 rounded-md px-3 py-2 text-sm font-semibold hover:bg-mist"><UserRound size={15} className="text-river"/>Add team member</Link></motion.div>}</AnimatePresence></div><div className="relative"><button type="button" className="icon-button relative" onClick={() => setNotificationsOpen((value) => !value)} aria-label="Open notifications"><Bell size={18}/>{notifications.some((item) => !read.includes(item.title)) && <span className="absolute right-1.5 top-1.5 h-2 w-2 rounded-full border border-white bg-amber"/>}</button><AnimatePresence>{notificationsOpen && <motion.div initial={{ opacity: 0, y: -5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -4 }} className="absolute right-0 top-full z-30 mt-2 w-80 rounded-lg border border-border bg-white p-2 shadow-panel"><div className="flex items-center justify-between px-2 py-1"><strong className="text-sm text-charcoal">Operations notifications</strong><button type="button" onClick={() => setRead(notifications.map((item) => item.title))} className="text-xs font-semibold text-river">Mark all read</button></div>{notifications.map((item) => <button type="button" key={item.title} onClick={() => { setRead((current) => [...new Set([...current, item.title])]); go(item.to); }} className="mt-1 block w-full rounded-md px-2 py-2 text-left hover:bg-mist"><span className={`block text-sm font-semibold ${read.includes(item.title) ? "text-stone" : "text-charcoal"}`}>{item.title}</span><span className="block text-xs text-stone">{item.detail}</span></button>)}<p className="px-2 pt-2 text-[11px] text-stone">Frontend preview notifications only.</p></motion.div>}</AnimatePresence></div><div className="relative"><button type="button" className="flex h-9 items-center gap-2 rounded-md border border-border bg-white px-2 hover:bg-mist" onClick={() => setProfileOpen((value) => !value)}><span className="grid h-7 w-7 place-items-center rounded-md bg-mist text-xs font-bold text-pine">AK</span><span className="hidden text-left lg:block"><strong className="block text-xs text-charcoal">Asim Khan</strong><small className="block text-[10px] text-stone">Platform admin</small></span><ChevronDown size={13} className="text-stone"/></button><AnimatePresence>{profileOpen && <motion.div initial={{ opacity: 0, y: -5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -4 }} className="absolute right-0 top-full mt-2 w-52 rounded-lg border border-border bg-white p-2 shadow-panel"><strong className="block px-2 py-1 text-sm text-charcoal">Asim Khan</strong><p className="px-2 pb-2 text-xs text-stone">Profile and sign-out require auth later.</p><Link to="/settings" onClick={() => setProfileOpen(false)} className="block rounded-md px-2 py-2 text-sm font-semibold hover:bg-mist">Account settings</Link></motion.div>}</AnimatePresence></div></div></header>;
}
