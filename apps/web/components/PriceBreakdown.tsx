export function PriceBreakdown({ price }: { price: number }) { return <div className="rounded-brand border border-border bg-white p-5"><div className="flex justify-between text-sm text-stone"><span>Package starting price</span><span>PKR {price.toLocaleString()}</span></div><div className="my-3 border-t border-border"/><div className="flex justify-between font-display text-lg font-bold text-pine"><span>Estimated total</span><span>PKR {price.toLocaleString()}</span></div><p className="mt-3 text-xs text-stone">Final pricing is confirmed by phone based on dates, group size, and availability.</p></div>; }
