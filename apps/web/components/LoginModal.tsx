"use client";
import { X } from "lucide-react";
import { AnimatePresence, motion } from "framer-motion";
import Link from "next/link";
export function LoginModal({ open, onClose }: { open: boolean; onClose: () => void }) { return <AnimatePresence>{open && <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 grid place-items-center bg-charcoal/55 p-4" onClick={(event) => event.target === event.currentTarget && onClose()}><motion.div initial={{ opacity: 0, scale: .96, y: 12 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: .97, y: 8 }} transition={{ duration: .22 }} className="relative w-full max-w-md rounded-brand border border-border bg-snow p-7 shadow-editorial"><button className="absolute right-4 top-4 text-stone" onClick={onClose} aria-label="Close"><X size={19}/></button><div className="eyebrow">WELCOME BACK</div><h2 className="font-display text-3xl font-bold">Log in to SwatStay</h2><p className="my-3 text-sm text-stone">Tourist and provider accounts will be connected in the next prototype phase.</p><Link href="/login" onClick={onClose} className="button w-full bg-pine text-white">Open login form</Link></motion.div></motion.div>}</AnimatePresence>; }
