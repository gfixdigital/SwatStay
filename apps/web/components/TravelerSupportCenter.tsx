"use client";

import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { ArrowLeft, CheckCircle2, LifeBuoy, MessageCircle, PhoneCall, Send } from "lucide-react";
import { createSupportTicket, getMySupport, sendSupportMessage } from "@/lib/api";

type Request = { id: string; topic: string; status: "Open" | "In review" | "Resolved"; updated: string; messages: { sender: "You" | "GFix team"; body: string; time: string }[] };
type ApiTicket = { id: string; subject: string; issueType: string; status: string; updatedAt: string; messages?: Array<{ body: string; createdAt: string; authorId?: string | null; author?: { fullName?: string } | null }> };
const initial: Request[] = [{ id: "SUP-82", topic: "Meal preference", status: "Open", updated: "35 minutes ago", messages: [{ sender: "You", body: "Please confirm vegetarian dinner with the hotel before arrival.", time: "Today, 10:15 AM" }, { sender: "GFix team", body: "We have added this to your hotel request. We will confirm it before departure.", time: "Today, 10:30 AM" }] }];

function mapTicket(ticket: ApiTicket): Request { return { id: ticket.id, topic: ticket.subject || ticket.issueType, status: ticket.status === "RESOLVED" || ticket.status === "CLOSED" ? "Resolved" : ticket.status === "IN_PROGRESS" ? "In review" : "Open", updated: new Date(ticket.updatedAt).toLocaleString(), messages: (ticket.messages ?? []).map((message) => ({ sender: message.authorId ? "You" : "GFix team", body: message.body, time: new Date(message.createdAt).toLocaleString() })) }; }

export function TravelerSupportCenter() {
  const [requests, setRequests] = useState<Request[]>(initial);
  const [selectedId, setSelectedId] = useState(initial[0].id);
  const [message, setMessage] = useState("");
  const [topic, setTopic] = useState("Booking question");
  const [error, setError] = useState("");
  const [loading, setLoading] = useState(true);
  const selected = requests.find((request) => request.id === selectedId) ?? requests[0];

  useEffect(() => { void getMySupport<ApiTicket>().then((tickets) => { if (tickets.length) { const mapped = tickets.map(mapTicket); setRequests(mapped); setSelectedId(mapped[0].id); } }).catch((reason: unknown) => setError(reason instanceof Error ? reason.message : "Could not load support requests.")).finally(() => setLoading(false)); }, []);

  async function send(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!message.trim() || !selected) return; const body = message.trim(); setError(""); try { if (selected.id.startsWith("SUP-")) throw new Error("demo"); await sendSupportMessage(selected.id, body); setRequests((current) => current.map((request) => request.id === selected.id ? { ...request, status: "In review", updated: "Just now", messages: [...request.messages, { sender: "You", body, time: "Just now" }] } : request)); } catch (reason: unknown) { if (reason instanceof Error && reason.message === "demo") setError("This sample request is not connected to an account. Create a new request to start a live support thread."); else setError(reason instanceof Error ? reason.message : "Unable to send your message."); } setMessage(""); }

  async function createRequest() { setError(""); try { const created = await createSupportTicket({ subject: topic, issueType: topic, message: `New support request: ${topic}` }) as ApiTicket; const mapped = mapTicket(created); setRequests((current) => [mapped, ...current]); setSelectedId(mapped.id); } catch (reason: unknown) { if (reason instanceof Error && reason.message.includes("log in")) setError("Log in to create a live support request."); else setError(reason instanceof Error ? reason.message : "Unable to create support request."); } }

  return <main className="bg-snow py-6 md:py-10"><div className="container max-w-6xl"><Link href="/dashboard" className="inline-flex items-center gap-1 text-sm font-semibold text-river hover:text-pine"><ArrowLeft size={15}/> Back to trip dashboard</Link><div className="mt-4 flex flex-col justify-between gap-3 border-b border-border pb-4 sm:flex-row sm:items-end"><div><div className="eyebrow">BOOKING SUPPORT</div><h1 className="font-display text-2xl font-bold text-pine md:text-3xl">Help and support</h1><p className="mt-1 max-w-2xl text-sm leading-6 text-stone">Send a request, review the conversation, or contact the GFix trip desk.</p></div><div className="flex gap-2"><a href="tel:+92946000000" className="button min-h-9 border-border bg-white px-3 text-xs text-pine hover:bg-mist"><PhoneCall size={15}/> Call</a><a href="https://wa.me/92946000000" className="button min-h-9 bg-pine px-3 text-xs text-white"><MessageCircle size={15}/> WhatsApp</a></div></div>{(error || loading) && <p className="mt-4 rounded-md border border-border bg-white p-3 text-sm text-stone">{loading ? "Loading support requests..." : error}</p>}<div className="mt-5 grid gap-4 lg:grid-cols-[300px_1fr]"><aside className="rounded-brand border border-border bg-white p-3"><label className="text-xs font-semibold text-stone">New request topic<select value={topic} onChange={(event) => setTopic(event.target.value)} className="field mt-1.5 w-full"><option>Booking question</option><option>Payment question</option><option>Pickup timing</option><option>Hotel</option><option>Transport</option><option>Guide or activity</option><option>Documents</option></select></label><button type="button" onClick={() => void createRequest()} className="button mt-2 min-h-9 w-full bg-pine px-3 text-xs text-white hover:bg-[#0e2c22]"><LifeBuoy size={15}/> Create request</button><div className="mt-4 space-y-2">{requests.map((request) => <button key={request.id} type="button" onClick={() => setSelectedId(request.id)} className={`w-full rounded-md border p-3 text-left ${request.id === selected?.id ? "border-pine bg-mist" : "border-border hover:bg-snow"}`}><div className="flex items-center justify-between gap-2"><strong className="text-xs text-river">{request.id}</strong><span className="text-[11px] text-stone">{request.status}</span></div><span className="mt-1 block text-sm font-semibold text-charcoal">{request.topic}</span><span className="mt-1 block text-xs text-stone">{request.updated}</span></button>)}</div></aside><section className="rounded-brand border border-border bg-white"><div className="border-b border-border p-4"><div className="flex items-center justify-between gap-3"><div><h2 className="font-display text-lg font-bold text-charcoal">{selected?.topic ?? "No support request selected"}</h2><p className="mt-1 text-xs text-stone">{selected?.id ?? ""} · {selected?.status ?? ""}</p></div><span className="inline-flex items-center gap-1 rounded-md bg-mist px-2 py-1 text-xs font-semibold text-pine"><CheckCircle2 size={13}/> Request history</span></div></div><div className="min-h-64 space-y-3 p-4">{selected?.messages.map((item, index) => <div key={`${item.time}-${index}`} className={`max-w-[85%] rounded-md p-3 text-sm leading-5 ${item.sender === "You" ? "ml-auto bg-mist text-charcoal" : "bg-snow text-charcoal"}`}><strong className="block text-xs text-river">{item.sender}</strong><p className="mt-1">{item.body}</p><span className="mt-1 block text-[11px] text-stone">{item.time}</span></div>)}</div><form onSubmit={send} className="border-t border-border p-3"><label className="sr-only" htmlFor="support-message">Message</label><div className="flex gap-2"><input id="support-message" value={message} onChange={(event) => setMessage(event.target.value)} className="field min-w-0 flex-1" placeholder="Write a message for GFix support" required/><button className="button min-h-10 bg-pine px-3 text-xs text-white hover:bg-[#0e2c22]"><Send size={15}/> Send</button></div><p className="mt-2 text-xs text-stone">Messages are delivered to the protected support workspace when you are logged in.</p></form></section></div></div></main>;
}
